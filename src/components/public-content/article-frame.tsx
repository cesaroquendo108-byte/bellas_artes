import Link from "next/link"
import { ArrowLeft, BookOpen, CalendarDays, UserRound } from "lucide-react"
import { Badge } from "@/components/ui/badge"
import type { ContentMetadataBase } from "@/lib/content/types"
import { CoverArt } from "./cover-art"

export function ArticleFrame({ metadata, backHref, backLabel, children }: { metadata: ContentMetadataBase & { category: string }; backHref: string; backLabel: string; children: React.ReactNode }) { return <article className="mx-auto max-w-5xl px-4 py-12 sm:px-7 sm:py-16"><Link href={backHref} className="inline-flex items-center gap-2 text-xs text-slate-500 hover:text-white"><ArrowLeft className="size-4" /> {backLabel}</Link><header className="mx-auto mt-10 max-w-3xl text-center"><Badge className="bg-violet-500/10 text-violet-300">{metadata.category}</Badge><h1 className="mt-6 text-4xl font-semibold tracking-tight sm:text-6xl">{metadata.title}</h1><p className="mx-auto mt-5 max-w-2xl text-sm leading-7 text-slate-500">{metadata.excerpt}</p><div className="mt-6 flex flex-wrap justify-center gap-4 text-[10px] text-slate-600"><span className="flex items-center gap-1"><UserRound className="size-3" />{metadata.author}</span><span className="flex items-center gap-1"><CalendarDays className="size-3" />{metadata.publishedAt}</span>{metadata.readingTime && <span className="flex items-center gap-1"><BookOpen className="size-3" />{metadata.readingTime}</span>}</div></header><CoverArt tone={metadata.cover} className="mt-10 aspect-[16/7] rounded-3xl border border-white/[0.08]" /><div className="mx-auto max-w-3xl pb-16 pt-6">{children}</div></article> }
